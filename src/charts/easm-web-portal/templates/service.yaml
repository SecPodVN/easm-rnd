apiVersion: v1
kind: Service
metadata:
  name: {{ include "easm-frontend.fullname" . }}
  labels:
    {{- include "easm-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "easm-frontend.selectorLabels" . | nindent 4 }}
