apiVersion: v1
kind: Service
metadata:
  name: {{ include "easm-user-portal.fullname" . }}
  labels:
    {{- include "easm-user-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "easm-user-portal.selectorLabels" . | nindent 4 }}
